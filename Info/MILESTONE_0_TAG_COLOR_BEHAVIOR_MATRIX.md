# Milestone 0 Tag Color Behavioral Coverage Matrix

This matrix is the behavioral inventory for PR #85. It describes the permanent persistence-neutral contract for `TagColorRepository` before the Core Data implementation exists. Contract assertions are present in `yaHerdTests/RepositoryContracts/TagColorRepositoryContract.swift`; executable persistence verification is deferred to the future Core Data contract runner per `AGENTS.md`.

| Production operation / behavior | Production callers | Success path | Error / rollback path | Clear / reset behavior | Batch / mixed-input behavior | Same-context behavior | Fresh-context reload | Production read projections | Relationships / history | Cross-feature effects | Unrelated control records | Identity / metadata | Contract owner |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Fetch visible tag-color library, including pristine built-ins and fallback default | `TagColorLibraryStore.init/load/refresh`; `TagColorLibraryView`; tag formatting/resolution helpers used by animal/pasture/working/field-check presentation | Covered: `assertBuiltInLibraryHasStableIdentityOrderingAndWhiteDefault` requires canonical built-ins, ordering, and White as the single pristine default | Unverified: persistence read failures are surfaced by `TagColorLibraryStore.lastErrorMessage`; executable Core Data failure injection does not exist in Milestone 0 | N/A: fetch is read-only; reset is owned by restore-default behavior below | N/A: one complete library read, not a batch mutation | Covered: first repository read is asserted before any reload | Covered: a fresh repository instance must return the same stable projection | Covered: `fetchColors()` projection includes ID, name, prefix, RGBA, sort order, and default state | N/A: fetch does not mutate relationships/history | Covered: stable IDs/default fallback support formatted animal tags and historical display consumers | Covered: pristine read asserts only the selected Herd's visible library; cross-Herd isolation is covered in the Herd-scope row | Covered: built-in UUIDs, order, visible definition, and default state are stable | This PR |
| Create or edit a custom color through upsert, including normalization | `TagColorEditorView` save -> `TagColorLibraryStore.upsert` -> `TagColorRepository.upsert`; mutation wrapper publishes after successful write | Covered: `assertUpsertNormalizesPersistsAndPreservesApplicationIdentity` checks trimmed name, normalized/fallback prefix, RGBA update, and durable edit | Covered: whitespace-only input is a no-op via `assertEmptyNameUpsertIsNoOp`; missing/stale Herd failure is covered by the Herd-scope failure row; generic failed-save rollback/publication is delegated to MutationBoundary/transaction contracts | Covered: blank prefix resets to generated prefix during edit; whitespace-only name does not mutate library | N/A: upsert is one logical color mutation | Covered: inserted and updated definitions are immediately readable through a new repository instance; presentation refresh is handled by `TagColorLibraryStore.performRepositoryWrite` | Covered: updated definition, UUID, and metadata survive fresh repository construction | Covered: visible library projection is re-fetched after write by `TagColorLibraryStore` | Covered: collision/remap rows below own relationship effects caused by identity reconciliation | Covered: edited color definition is consumed by tag formatting across animal, pasture, working, and field-check displays | Covered: cross-Herd row snapshots Herd A and proves Herd B upserts do not mutate it | Covered: application UUID and original `createdAt` remain stable; `updatedAt` does not regress; new ordering is repository-owned | This PR; generic mutation publication owned by `MutationBoundaryContract` |
| Resolve normalized-name collisions between custom colors | `TagColorEditorView` save -> upsert; repository duplicate-reconciliation path | Covered: `assertNormalizedNameCollisionKeepsCanonicalIdentityAndRemapsReferences` requires one normalized-name result and existing canonical UUID retention | Delegated: physical save failure/transaction rollback belongs to future Core Data transaction/fault-injection coverage | N/A: collision reconciliation is not a clear/reset action | Covered: representative duplicate pair exercises mixed canonical/incoming IDs; equivalent duplicate permutations are the same invariant | Covered: post-reconciliation read must expose one canonical color immediately | Covered: fresh repository read must no longer expose incoming duplicate UUID | Covered: visible library contains one normalized-name definition | Covered: current animal tag, historical tag, Field Check roster tag, dam roster tag, finding snapshot, Working queue animal tag, and Working queue dam-tag references are remapped to canonical UUID | Covered: prevents fallback-color regressions in Animal/Field Check history and tag display | Covered: unrelated Herd isolation is owned by the Herd-scope row | Covered: canonical application UUID survives; incoming duplicate UUID is eliminated; default state follows the canonical merged record | This PR |
| Resolve normalized-name collisions against application built-ins | `TagColorEditorView` can save names equivalent to built-ins; repository duplicate-reconciliation path | Covered: `assertBuiltInNameCollisionPreservesBuiltInIdentityAndRemapsReferences` requires normalized `Blue` to retain `TagColorDefaults.blueID` while applying the incoming normalized display definition | Delegated: physical save failure/rollback belongs to future Core Data transaction coverage | N/A: collision reconciliation is not reset behavior | Covered: representative virtual/materialized built-in collision protects the distinct built-in-identity path | Covered: result is immediately one visible normalized built-in identity | Covered: incoming custom UUID must remain absent on fresh read | Covered: visible built-in remains resolvable by its stable UUID | Covered: all persisted tag-color reference categories, including Field Check and Working queue snapshots, are remapped from incoming UUID to the built-in UUID | Covered: records already using built-in UUID and records using incoming duplicate converge on one identity | Covered: Herd isolation is separately asserted and unchanged by built-in collision handling | Covered: stable built-in UUID is authoritative even when no physical built-in row existed before the collision | This PR |
| Select and preserve the default color | `TagColorEditorView`; `TagColorLibraryView` swipe/context actions -> `TagColorLibraryStore.setDefaultColor`; default fallback used by tag resolution | Covered: `assertDefaultSelectionIsExclusivePersistentAndUnaffectedByUnknownIDs` requires exactly one selected default and persistence across edits/reloads | Covered: unknown UUID is a no-op; missing/stale Herd write rejection is covered by the Herd-scope failure row; generic failed-save rollback/publication delegated | Covered: deleting a selected materialized built-in falls back to White in deletion row; editing selected custom with `isDefault=false` does not implicitly clear selection | N/A: one UUID is selected at a time; unknown-ID mixed behavior is represented by explicit no-op case | Covered: repository read after selection reports exactly one default | Covered: selected custom/built-in default survives fresh repository construction | Covered: `TagColorLibraryStore.defaultColor/defaultColorID/resolvedColorID` depend on this projection | Covered: default selection does not rewrite existing tag/history references | Covered: new/legacy tag fallback display uses the selected default | Covered: cross-Herd row proves Herd B default changes do not alter Herd A | Covered: default UUID is stable and exclusive; edit does not replace color identity | This PR |
| Resolve a removed/hidden referenced color by application UUID for historical display | `TagColorLibraryStore.definition/resolvedDefinition/resolvedColorID/formattedTag`; retired-tag, Field Check, and Working history views that retain color UUID snapshots | Covered: repository contract requires a removed referenced custom definition to remain retrievable by UUID outside the visible library; presentation test requires the store to use that lookup rather than default fallback | Unverified: Core Data lookup failure injection does not exist in Milestone 0; the nonthrowing presentation resolver falls back to the default if persistence lookup fails | N/A: this is a read projection; visible restore/re-add behavior is owned by upsert/restore rows | N/A: one application UUID is resolved at a time | Covered: hidden definition lookup is available immediately after visible removal | Covered: a fresh repository must resolve the same hidden definition by stable UUID | Covered: `TagColorLibraryStore` must preserve original prefix/RGBA for hidden referenced colors instead of resolving them as the current default | Covered: historical AnimalTag, Field Check, and Working queue UUID snapshots retain their semantic display definition after visible removal | Covered: retired-tag, Field Check, and Working history presentation continues to render the captured color identity | Covered: lookup is Herd-scoped; cross-Herd isolation remains owned by the Herd-scope row | Covered: hidden definition keeps original application UUID/name/prefix/RGBA while no longer appearing in `fetchColors()` | This PR |
| Remove colors from the visible library while preserving historical identity | `TagColorLibraryView.onDelete` -> `TagColorLibraryStore.delete` -> `deleteColors(ids:)` | Covered: `assertDeleteRemovesCustomColorsButBuiltInsRemainAvailableAndDefaultFallsBackToWhite` and `assertReferencedCustomColorRemovalPreservesHistoricalReferenceIdentity` | Delegated: persistence delete/hide transaction failure rollback belongs to future Core Data transaction coverage; missing/stale Herd failure covered by Herd-scope row | Covered: deleting selected built-in materialization restores visible canonical built-in and White default fallback | Covered: delete accepts ID arrays; representative custom plus built-in/unknown IDs covers valid, virtual, and unknown identity handling | Covered: removed custom is absent from visible library immediately while historical reference IDs remain unchanged | Covered: fresh read keeps custom removed from visible library and built-ins available | Covered: `fetchColors()` visibility is the user-facing removal projection | Covered: current/historical tag, Field Check snapshot, and Working queue snapshot UUIDs remain intact for referenced custom color removal; the historical-definition projection row requires the hidden definition itself to remain resolvable | Covered: historical tag/Field Check/Working display retains the removed color's original prefix/RGBA through the historical-definition lookup; default fallback remains for genuinely unknown IDs | Covered: cross-Herd row proves Herd B delete leaves Herd A projection unchanged | Covered: built-in UUIDs cannot be removed from application constants; referenced custom UUID identity is preserved in history | This PR |
| Reorder visible colors | `TagColorLibraryView.onMove` -> `TagColorLibraryStore.move` -> `reorder(colorIDs:)` | Covered: `assertReorderPersistsCompleteLibraryOrder` requires requested complete order and contiguous sort values | Delegated: save rollback and mutation publication belong to future Core Data transaction coverage and `MutationBoundaryContract`; missing/stale Herd failure covered by Herd-scope row | N/A: reorder has no clear/reset state; restore defaults is separate | Covered: complete ordered ID list is the production batch shape | Covered: reordered list is observable immediately after mutation | Covered: order survives fresh repository construction | Covered: `TagColorLibraryView` consumes repository sort order directly | N/A: reorder must not change tag relationships/history | N/A: only library presentation order changes | Covered: cross-Herd row proves Herd B reorder leaves Herd A projection unchanged | Covered: only `sortOrder` changes; IDs/default/reference identity remain stable | This PR |
| Restore canonical built-in definitions without deleting custom state | `TagColorLibraryView` Restore Default Colors -> `TagColorLibraryStore.restoreDefaultColors` | Covered: `assertRestoreDefaultsRepairsCanonicalDefinitionsPreservesCustomsAndIsIdempotent` restores every canonical built-in and preserves custom color/default | Delegated: save rollback/publication belongs to future Core Data transaction coverage and `MutationBoundaryContract`; missing/stale Herd failure covered by Herd-scope row | Covered: this is the explicit reset operation; repeated reset is idempotent | Covered: reset operates over the complete built-in set while preserving custom entries | Covered: first restore exposes repaired canonical built-ins immediately | Covered: repeated restore/fresh repository returns the same Domain-visible projection | Covered: visible library definitions are checked against `TagColorDefaults.seedDefaultColors()` | Covered: restore does not intentionally rewrite tag/history references because canonical built-in UUIDs remain stable | Covered: consumers continue resolving built-ins by stable IDs and custom default remains selected | Covered: cross-Herd row proves Herd B restore leaves Herd A projection unchanged | Covered: built-in UUID/name/prefix/RGBA are canonical; custom UUID/default identity is preserved | This PR |
| Enforce current-Herd scope for all tag-color reads and mutations | Repository instances resolved through application persistence assembly; all `TagColorLibraryStore` reads/writes are Herd-owned | Covered: `assertReadsWritesDefaultsAndNameUniquenessAreHerdScoped` proves per-Herd reads, writes, default, normalized-name uniqueness, upsert, reorder, delete, and restore isolation | Covered: `assertMissingOrStaleCurrentHerdDoesNotFallbackOrBootstrapOnReadOrWrite` requires `fetchColors`, upsert, default selection, delete, reorder, and restore to reject both nil and stale Herd selections without fallback/bootstrap; generic save rollback delegated | N/A: current-Herd selection lifecycle is owned by `HerdRepositoryContract`; this row only consumes selection state | Covered: representative all-mutation sequence in Herd B is compared against an unchanged Herd A control projection | Covered: switching back to Herd A after Herd B mutations must reproduce the control projection | Covered: fresh repository factories under each selected Herd expose only that Herd's state | Covered: visible library/default/name lookup semantics are scope-local | Covered: persisted tag-color relationships must remain attached to their owning Herd; cross-Herd reference validity is a Core Data integrity responsibility | Covered: prevents settings changes in one Herd from changing tag display/data in another | Covered: Herd A stable projection and persisted Herd-row counts are explicit controls | Covered: same normalized name may exist in different Herds with different UUIDs; failed writes do not create/duplicate Herd roots | This PR; Herd selection/root multiplicity owned by `HerdRepositoryContract` |
| Publish mutation events only after successful logical writes | `MutationPublishingTagColorRepository` wraps upsert/default/delete/reorder/restore | Delegated: successful-write publication ordering is owned by `MutationBoundaryContract` | Delegated: failed writes must not publish success; rollback/event ordering is owned by `MutationBoundaryContract` and future Core Data transaction coverage | N/A: publication has no reset lifecycle | Covered: all five write entry points pass through the same mutation wrapper | Delegated: same-context event visibility is owned by `MutationBoundaryContract` | N/A: mutation event publication is runtime behavior, not persisted reload state | N/A: no repository read projection is changed solely by publication | N/A: relationship correctness remains in the operation rows above | Covered: presentation invalidation after writes depends on mutation publication but invariant ownership is delegated | N/A: event contract does not mutate unrelated persisted records | N/A: publication does not own application identity/timestamps | `MutationBoundaryContract` |
